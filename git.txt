git checkout main          # main 브랜치로 이동
git branch                 # 현재 branch 확인
git checkout -b 브랜치명    # main에서 파생된 브랜치를 만들고 이동

'''
    작업, 코드 수정...
'''

git add .                   # 수정, 추가된 모든 파일 스테이징
git commit -m "..."         # 커밋 메시지
git push origin 브랜치명     # 브랜치에 저장

git checkout main           # main 브랜치로 이동
git pull origin main        # main브랜치 최신으로 업데이트

git merge --no-ff 브랜치명   # '--no-ff' 병합내역 명시적으로 남김(생략가능)
    # 충돌시 파일수정 후
    # git add 파일명
    # git commit

git push origin main        # 수정, 추가한 브랜치 main과 병합합